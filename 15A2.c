#include<stdio.h>
void main(){
    int i,n,negative=0;
    printf("enter val of array:");
    scanf("%d",&a[i]);
    int a[n];
    for(i=0;i<n;i++){
        printf("enter val:");
        
    }
}